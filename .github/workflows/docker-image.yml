name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: self-hosted

    steps:
    - run: docker build . -t show-me-recipes:latest 
    - run: docker stop show-me-recipes
    - run: docker rm show-me-recipes
    - run: docker run -d --name show-me-recipes -p 80:80 show-me-recipes:latest
