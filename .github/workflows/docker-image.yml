name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - run: docker build . --file Dockerfile --tag show-me-recipes:latest 
    - run: docker stop show-me-recipes || true && docker rm show-me-recipes || true
    - run: docker run -d --name show-me-recipes -p 80:80 show-me-recipes:latest
